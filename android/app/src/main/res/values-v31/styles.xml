<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ (API 31+) Native SplashScreen Configuration -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Background color behind splash icon -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>

        <!-- Splash Icon meeting Android 12 specifications (pure VectorDrawable XML) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_metrology_scale</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_metrology_scale</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="android:windowSplashScreenAnimationDuration">1000</item>

        <!-- Theme to switch to once splash exits -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>

        <!-- Edge-to-edge transparent system bars and notch support -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
